pipeline {
    agent any

    stages {
        stage('Init') {
            steps {
                echo "Iniciando pipeline QA 🚀"
            }
        }

        stage('Build') {
            steps {
                echo "Simulando build..."
            }
        }

        stage('Test') {
            steps {
                echo "Tests OK ✅"
            }
        }
    }
}